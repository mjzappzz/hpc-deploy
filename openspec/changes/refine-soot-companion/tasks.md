## 1. 陪伴规则与测试基线

- [x] 1.1 为煤球偏好解析、单实例状态转换、低频回应和减少动效行为编写先失败的前端单元/源码回归测试，并验证现有随机多角色实现不能满足这些断言。
- [x] 1.2 为允许宿主、管理员确认/危险操作/错误告警排除、窄视口气泡回退和卸载资源清理编写回归测试，并验证测试覆盖正常、安静、关闭三种模式。
- [x] 1.3 为应用级安全悬浮位、全路由常驻、危险 UI 静默、任务终态语义和毛绒视觉钩子补充回归测试；先验证现有卡片宿主和通用刷新事件不能满足断言。

## 2. 单煤球陪伴核心

- [x] 2.1 实现固定名称“煤球”的单实例状态机与受控 Vue 渲染，删除随机名字、双煤球对话及不可达辅助逻辑；验证全局任何时刻最多一个实例。
- [x] 2.2 实现探头进入、安静值守、上下文回应和视口迁移退出的 CSS 状态，并验证不再按固定短生命周期随机替换煤球。
- [x] 2.3 实现正常陪伴、安静陪伴、关闭的浏览器本地偏好及减少动效覆盖；验证关闭/减少动效时无煤球、气泡、观察器或活动计时器残留。
- [x] 2.4 将煤球从卡片宿主迁移至根应用安全悬浮位，在普通路由持续保留同一实例；实现危险 UI 的静默与气泡收起，并验证不遮挡交互控件。
- [x] 2.5 恢复约 26px 的毛绒煤球视觉（蓬松毛边、立体黑灰渐变与轻量呼吸动效），并验证触屏与减少动效边界。

## 3. 页面上下文与安全投放

- [x] 3.1 为业务页面建立显式煤球宿主注册/标记，只接入经过确认的普通业务卡片；验证未标记卡片默认不投放。
- [x] 3.2 建立只读陪伴上下文，接入路由与明确的任务创建、运行、完成、恢复事件；验证上下文缺失时退回安静值守且不分析用户停留或点击行为。
- [x] 3.3 实现值守、收尾、轻幽默和静默文案分类，删除指向操作者的嘲讽、过度心理化和故障时玩笑；验证故障、告警、高风险操作、弹窗、抽屉和通知区域不投放。
- [x] 3.4 实现悬停与触屏轻触的主动回应，以及安全空间不足时隐藏气泡的回退；验证气泡不遮挡关键操作区域。
- [x] 3.5 将任务刷新事件改为传递明确终态；仅 `SUCCESS` 触发收尾文案，`FAILED` 与 `CANCELED` 保持静默。

## 4. 文档与交付验证

- [x] 4.1 更新 `docs/fun-principles.md`，记录单煤球设定、投放边界、文案边界、三档偏好和无障碍规则；验证文档不再描述随机命名或双煤球对话。
- [x] 4.2 更新 `docs/progress.md` 记录行为变化与发布状态，并验证文档与实现、规格一致。
- [x] 4.3 运行相关前端测试、`npm run build`，并在真实浏览器验证桌面/触屏入口、减少动效、全路由安全悬浮、窄视口、任务成功、失败/取消静默和危险 UI 场景；确认控制台无新增错误。

## 5. 默认陪伴与造型收束

- [x] 5.1 补充先失败的回归测试，覆盖顶栏不再提供煤球模式选择、减少动效仍隐藏煤球，以及毛边不在脚下形成连续扩散层。
- [x] 5.2 移除煤球正常/安静/关闭的本地偏好与顶栏选择器，保留减少动效、危险 UI 静默、任务成功回应和用户主动回应。
- [x] 5.3 将煤球重绘为圆脑袋与两只清晰短脚，消除底部泥状毛边，并更新设计规范、变更规格和进度记录。
- [x] 5.4 运行相关源码测试、生产构建和 Playwright 桌面/触屏验证，确认煤球常驻、无模式选择、减少动效隐藏且控制台无新增错误。
