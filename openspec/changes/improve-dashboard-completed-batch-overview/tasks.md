## 1. 后端批次摘要

- [x] 1.1 盘点并复用任务历史已有的批次查询、整体状态和最终结果判定逻辑，验证单次任务、批次任务和报告级失败语义一致
- [x] 1.2 为 Dashboard 增加近期完成批次摘要响应结构，包含批次 ID、类型、服务器数、子任务总数、成功/失败/取消统计、整体状态、创建/结束时间和耗时，验证接口模型序列化通过
- [x] 1.3 实现按完整批次聚合的查询与排序，避免使用最近子任务 limit 反推批次，验证批次子任务不完整时摘要仍覆盖全部子任务
- [x] 1.4 为批次聚合增加单元/集成回归测试，覆盖全部成功、部分失败、报告级失败、取消、混合单次任务和无批次任务

## 2. Dashboard 展示

- [x] 2.1 更新 Dashboard API 类型和旧后端兼容降级，验证缺少 `recent_completed_batches` 时仍显示单次完成任务
- [x] 2.2 将近期完成数据转换为批次/单次混合展示条目，验证一个批次只显示一行且显示数量按聚合条目计算
- [x] 2.3 按现有表格格式增加批次摘要列和“查看批次详情”入口，验证批次状态、成功/失败摘要、服务器数、子任务数和时间信息直观可见
- [x] 2.4 保持运行中任务逐子任务展示、刷新、空态和跳转行为不变，验证现有运行中任务测试通过
- [x] 2.5 增加 Dashboard 源码/组件测试，覆盖批次与单次混排、部分失败、详情跳转、显示数量和窄屏布局

## 3. 文档与交付验证

- [x] 3.1 更新 `docs/architecture.md`，记录 Dashboard 完成任务的批次聚合口径和运行中任务保持逐子任务展示的边界
- [x] 3.2 更新 `docs/progress.md`，记录批次整体摘要整改、兼容性和验证结果
- [x] 3.3 运行后端全量测试、前端测试、生产构建、`git diff --check` 和接口响应抽样，验证 Dashboard 响应耗时没有因批次聚合回到超时范围
- [ ] 3.4 在测试环境查看 Dashboard，确认批次整体行、单次行和现有运行中任务均符合预期；记录发布状态，未经确认不发布线上
