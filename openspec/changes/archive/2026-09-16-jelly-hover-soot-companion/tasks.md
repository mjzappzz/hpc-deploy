## 1. 回归测试

- [x] 1.1 为悬停动作与说话解耦、果冻压扁/拉伸/过冲序列、快速重复进入、点击仍可说话、减少动效禁用形变补充先失败的源码/浏览器断言，并验证现有实现先失败。

## 2. 果冻交互实现

- [x] 2.1 将 `mouseenter`/`focus` 从手动文案回应中拆出，新增每次进入都可重播的单实例果冻动画令牌或状态，并验证鼠标快速进出不会丢失动作。
- [x] 2.2 保持点击/触屏轻触的互动吐槽与独立短冷却，确保悬停动作不修改自动独白、成功反馈或手动文案计时，并验证危险 UI 与 `prefers-reduced-motion` 边界。
- [x] 2.3 以 `transform` squash-and-stretch 实现约 220--320ms 的压扁、拉伸、过冲和回弹视觉，验证固定右下定位、气泡安全边界和现有成功/失败终态行为不回归。

## 3. 文档与验收

- [x] 3.1 更新 `docs/fun-principles.md` 与 `docs/progress.md`，记录果冻物理感、重复悬停反馈、动作与文案解耦及减少动效边界，并确认文案与动作规则一致。
- [x] 3.2 运行前端源码测试、生产构建、Playwright 桌面/触屏回归和 `git diff --check`，确认全部通过且无新增控制台错误。
