## 1. 共享路径与后端统计

- [x] 1.1 抽取系统设置已有本地/远端运行路径清单，并让系统设置继续保持现有展示；验证 settings API 与现有设置测试通过
- [x] 1.2 为本地路径统计增加文件数、逻辑大小、缺失/不可读/部分扫描状态和排除关系；验证固定 fixture 中模块合计不重叠且 symlink 不重复计量
- [x] 1.3 将 Dashboard storage summary 改为项目模块总量和模块明细，保留设备/挂载点与既有容量告警字段；验证 API 不再用整盘 used/total 作为项目比例
- [x] 1.4 覆盖空项目、单文件模块、缺失路径、权限失败和扫描异常；验证失败模块不伪装为零值或精确比例，其他模块仍返回

## 2. Dashboard 首版视觉

- [x] 2.1 更新前端 Dashboard storage 类型和兼容降级逻辑；验证旧 summary 缺少新增字段时页面不报错
- [x] 2.2 将容量圆盘改为项目总占用分段图，中心显示项目总量，图例显示模块大小/文件数/百分比；验证比例合计和零数据状态正确
- [x] 2.3 展示系统设置对应的本地模块路径和状态，保留设备/挂载点为辅助信息并明确不参与项目圆盘口径；验证实际路径与 settings API 一致
- [x] 2.4 删除独立“服务器”和“任务运行”大卡片，增加紧凑运行状态条/区块标题；验证在线/离线、运行中/等待中/取消中仍可见，任务表和跳转不受影响
- [x] 2.5 保留近期完成任务和结果信息并调整层级；验证成功/失败/取消状态及任务详情入口仍可用
- [ ] 2.6 验证加载中、统计不可用、部分失败、窄屏和 `prefers-reduced-motion` 等展示状态；验证无布局溢出和控制台错误

## 3. 文档与交付验证

- [x] 3.1 更新 `docs/architecture.md`，记录项目空间统计边界、模块清单、逻辑大小口径和远端路径排除规则；验证文档与 API 实现一致
- [x] 3.2 更新 `docs/progress.md`，记录本次 Dashboard 首版和兼容性边界；验证变更可追溯
- [x] 3.3 运行后端定向/全量测试、前端测试、生产构建和 `git diff --check`；验证新 summary 与 settings 路径抽样一致
- [ ] 3.4 在测试环境查看 Dashboard，核对项目总占用、模块合计、路径和运行状态，并记录发布状态；验证不执行线上发布或服务重启，除非获得单独确认
