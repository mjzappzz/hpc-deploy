## 1. 诊断契约与证据聚合

- [x] 1.1 定义版本化诊断记录、失败阶段、置信度、已确认事实、证据、待核查项和建议动作的数据契约，并验证敏感字段不会进入诊断响应或缓存。
- [x] 1.2 重构诊断聚合优先级：平台确认事件、结构化脚本事件、报告明确原因、任务错误和终态日志回退依次处理，并以单元测试验证低优先级关键词不能覆盖高优先级事实。
- [x] 1.3 调整报告摘要缓存和任务序列化，保留兼容的 `failure_reason`，同时返回分层诊断字段；验证历史任务没有结构化事件时得到保守的兼容结果。

## 2. 受控压测与 runner 事件

- [x] 2.1 为平台 runner 的取消、SSH、上传、artifact 回收和状态收口事件生成结构化诊断证据，并验证取消与回收失败不再降级为普通未知失败。
- [x] 2.2 为 GPU、CPU/内存和磁盘压测脚本在依赖检查、压测启动、报告生成和终态输出版本化诊断事件及可回收诊断文件；以 Shell 契约测试验证事件完整性和敏感数据边界。
- [x] 2.3 为极限压测分别记录 GPU、CPU/内存和编排层结果，验证整体汇总失败不会覆盖已确认的子模块失败事实。
- [x] 2.4 更新 `backend/scripts/` 相关 README/架构说明和 `docs/progress.md`，记录诊断事件版本、兼容回退和历史任务边界；验证文档与脚本行为一致。

## 3. API 与运维控制台展示

- [x] 3.1 扩展诊断 API 和前端类型，兼容现有字段并提供失败阶段、已确认事实、证据、待核查项、建议动作和置信度；验证已有任务详情调用不受破坏。
- [x] 3.2 更新任务历史卡片、单任务/批次详情和诊断弹窗：默认显示可执行短摘要，详情按“已确认事实—证据—待核查—建议动作”展示；以前端测试验证 `unknown` 不被渲染为确定根因。

## 4. 回归、历史兼容与发布验证

- [x] 4.1 以现有历史样本覆盖依赖安装失败、批量取消、启动后无报告、GPU 温度失败、报告 FAIL、artifact 回收失败和极限压测子模块失败，验证每个“已确认事实”均有直接证据。
- [x] 4.2 执行后端测试、相关 Shell 语法/契约测试、前端目标测试和 `npm run build`，验证诊断缓存重算与既有最终状态规则未回归。
- [x] 4.3 按项目发布流程部署后，对线上新旧任务抽样核对诊断 API、任务历史和完整日志；记录发布结果与未能确认的历史任务边界。
