## 1. 后端存储统计契约

- [x] 1.1 扩展 Dashboard storage schema 和前端共享类型，增加使用率、设备、挂载点、检查路径、分类占用及逐项状态字段；验证现有 summary 响应字段保持兼容且 schema 测试通过
- [x] 1.2 为固定 HPCDeploy 路径实现非重叠分类统计，覆盖产物、SQLite、备份、GPU 驱动库、backend/data 其余数据和项目/运行时文件；验证正常路径下分类字节数与抽样 `du` 结果一致
- [x] 1.3 实现容量基准路径的设备/挂载点解析和分区其他占用计算；验证 `/dev/sdd`、项目挂载点及差额在本机 fixture/集成测试中正确返回
- [x] 1.4 覆盖路径不存在、权限不足、挂载信息不可解析和部分扫描失败；验证接口返回明确状态/原因，不把失败项静默计为零且不影响其他 summary 字段
- [x] 1.5 保持现有 warning/blocked/unknown 阈值和任务提交阻断行为；验证容量状态回归测试以及 unknown 不告警、不展示伪造零值

## 2. Dashboard 信息层级与可视化

- [x] 2.1 更新 Dashboard storage 类型、格式化和状态映射；验证字节、百分比、路径及未知/部分失败状态在边界值下渲染稳定
- [x] 2.2 将控制器空间卡片移动到刷新区域之后、普通汇总卡片之前，加入可访问的环形容量图和总/已用/可用/使用率摘要；验证 Dashboard 组件测试和窄屏布局通过
- [x] 2.3 增加设备、挂载点、实际检查路径、分类图例及“分区其他占用”说明；验证每个分类有文字标签和状态，颜色不是唯一信息来源
- [x] 2.4 调整服务器/任务/结果首屏层级，保留在线/离线及运行中/排队信息，将任务结果摘要移至较低优先级的近期完成任务区域；验证现有任务数据和跳转入口仍可用
- [x] 2.5 验证 storage summary 加载中、统计不可用、告警和阻断状态的加载/空态/失败态；验证不会因新增字段缺失导致 Dashboard 报错

## 3. 文档与验证交付

- [x] 3.1 更新 `README.md` 或 `docs/architecture.md`，记录控制器空间统计口径、固定检查路径、差额分类和边界；验证文档与 API 字段/实现一致
- [x] 3.2 更新 `docs/progress.md`，记录本次能力、兼容性和验证结论；验证变更记录可追溯
- [x] 3.3 运行后端测试、前端测试和构建，并用 `git diff --check` 检查；验证新增容量看板在实际 summary 数据下无控制台错误
- [x] 3.4 按项目发布流程完成测试环境验证并记录发布状态；验证发布前后 summary、`df`/`du` 抽样和 unknown/告警场景结果一致
