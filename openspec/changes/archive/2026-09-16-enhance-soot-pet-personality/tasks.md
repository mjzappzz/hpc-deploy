## 1. 宠物人格与安全回归

- [x] 1.1 为自动独白、互动吐槽、成功收尾、毒舌对象边界、危险 UI 静默、减少动效与路由切换不重置计时补充先失败的 `AppCritters` 源码/行为测试，并验证现有实现不满足新增断言。
- [x] 1.2 将煤球文案重组为自动独白、主动互动与成功收尾池，加入允许的运维黑话和网络梗，并通过测试确认文案不评价操作者或团队、失败和告警不触发趣味文案。

## 2. 低频调度与宠物动作

- [x] 2.1 在 `AppCritters.vue` 实现首次安全进入后的一次回应及每次 10--20 分钟重新抽取的自动独白调度；危险 UI、减少动效和卸载取消调度，路由切换不重置调度，并通过计时器测试或可控时钟验证。
- [x] 2.2 为煤球实现安全场景的探头/张望、主动交互的短暂缩回和 `SUCCESS` 的一次轻跳，保持固定安全位、单实例、键盘访问与 `prefers-reduced-motion` 无实例边界，并通过源码与浏览器断言验证。
- [x] 2.3 为自动、手动和成功回应建立统一短冷却与气泡安全空间处理，防止悬停、点击、路由或事件刷屏，并验证气泡不可安全显示时不残留可见性状态。

## 3. 文档与验收

- [x] 3.1 更新 `docs/fun-principles.md` 与 `docs/progress.md`，记录毒舌煤球人格、文案禁区、10--20 分钟低频独白、动作和静默规则，并确认文档不再将煤球描述为纯安静陪伴或固定短延迟发言。
- [x] 3.2 运行相关前端源码测试、生产构建和 Playwright 桌面/触屏回归，验证正常页面低频陪伴、成功动作、危险 UI 静默、窄视口气泡避让及控制台无新增错误。
