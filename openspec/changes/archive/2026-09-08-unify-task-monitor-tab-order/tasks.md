## 1. 回归保护

- [x] 1.1 为任务执行页、普通历史详情和批次详情的运行中 stress 监控标签顺序添加前端测试，并在现状确认测试失败。

## 2. 统一标签呈现

- [x] 2.1 调整 `TaskHistory.vue` 的普通详情与批次详情，使 GPU 位于 CPU 与内存之前，并验证既有可见性和终态隐藏条件不变。
- [x] 2.2 调整 `TaskRunner.vue` 的 stress 标签顺序和中文标签文案，使其与历史详情一致，并验证面板名称和监控类型不变。

## 3. 验证

- [x] 3.1 运行新增及受影响的前端测试、类型检查和生产构建，验证通过且无回归。
- [x] 3.2 在浏览器中验证运行中 stress 任务的三个入口均显示“执行日志、GPU、CPU与内存、磁盘 I/O”。（已由用户在生产页面确认）
