## 1. 人格上下文与优先级骨架

- [x] 1.1 盘点现有路由、任务事件、节点状态和危险 UI 入口，定义 `CompanionContext` 输入与安全默认值；以源码测试验证字段缺失时不抛错
- [x] 1.2 实现统一动作优先级决策器和 token/timer 清理边界；以源码测试验证同一时刻只保留一个主动作且组件卸载清理所有 timer

## 2. 连续点击人格

- [x] 2.1 实现 3 秒点击窗口、次数归零和 1/2/3/4+ 次动作映射；以源码测试覆盖计数边界
- [x] 2.2 增加弹开、躲闪、装死 CSS 动作并保持右下安全位；以浏览器测试验证快速连续点击每次都有动作反馈
- [x] 2.3 接入独立说话冷却、文案去重和装死恢复；以浏览器测试验证动作不被气泡冷却吞掉且不刷屏

## 3. 设备状态拟态

- [x] 3.1 根据实际可用事件字段选择 GPU、排队、网络、磁盘和完成状态映射；以契约测试验证未知字段回退为静默或不确定性状态
- [x] 3.2 实现每个状态最多一个轻量动作/纸条/道具且不伪造精确指标；以浏览器测试验证状态切换不新增轮询或业务操作

## 4. 页面道具与日志纸条

- [x] 4.1 为 GPU、任务、节点和日志页面定义上下文到小道具的映射；以源码测试验证固定尺寸、单实例和安全区域约束
- [x] 4.2 使用 CSS 或小型内联 SVG 实现道具与纸条；以生产构建检查资源体积并以浏览器测试验证不遮挡控件

## 5. 夜班与不确定性模式

- [x] 5.1 实现夜间时段判断、降低主动反馈频率和动作幅度；以浏览器测试覆盖时段边界及恢复
- [x] 5.2 实现数据不完整时的装懂文案/静默回退；以源码测试验证不会猜测成功、失败或操作者状态

## 6. 无障碍、回归与分期发布

- [x] 6.1 更新 `prefers-reduced-motion`、键盘激活、`role=status`、危险 UI 静默和气泡避让测试；运行前端源码测试与 Playwright 回归
- [x] 6.2 更新 `docs/fun-principles.md`、`docs/progress.md`，记录分期行为、性能边界和兼容性；以文档检查确认内容与实现一致
- [x] 6.3 每期执行 `npm run build`、`git diff --check` 和前端独立发布脚本；记录 release、回滚路径与后端未重启状态
