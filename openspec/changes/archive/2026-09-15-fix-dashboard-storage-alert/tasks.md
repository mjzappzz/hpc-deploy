## 1. 仪表盘存储状态修复

- [x] 1.1 在 `Dashboard.vue` 的仪表盘成功响应处理中回填接口返回的 `storage` 字段，并验证 `pass` 状态显示真实可用空间、产物占用、SQLite 占用和最近清理结果。
- [x] 1.2 将容量告警可见性改为仅接受 `warning` 或 `blocked`，保留两者的既有告警级别与文案，并验证 `unknown` 不显示低空间告警或默认 `0 B` 统计。
- [x] 1.3 让容量统计在 `pass` 状态常驻显示，并为 `unknown` 状态显示“容量统计暂不可用”，验证统计可见性与状态语义一致。

## 2. 回归验证

- [x] 2.1 扩展 Dashboard 源码约束测试，覆盖存储状态回填和显式容量状态判断，并验证目标 Node 测试通过。
- [x] 2.2 执行 `npm run build`，验证 TypeScript 检查与前端生产构建通过。
- [x] 2.3 扩展回归测试覆盖常驻容量卡片与 `unknown` 不显示默认零值，并重新执行目标测试与前端构建。
