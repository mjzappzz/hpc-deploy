## 1. 极限压测脚本与远端编排

- [ ] 1.1 为 GPU、CPU/内存压测脚本增加兼容常规独立调用的受控 prepare、ready、共享 start barrier 与进程组上报接口，并以 Shell 语法检查及入口合约测试验证两种调用路径。
- [ ] 1.2 新增极限压测编排脚本，串行完成依赖安装和构建、并行等待两个模块 ready、同步释放负载屏障，并以脚本回归测试验证首次依赖安装与已缓存依赖复用。
- [ ] 1.3 在极限模式向 CPU/内存模块传递至少 15% 的物理内存安全保留线，并以测试验证该覆盖只作用于极限模式且安全线触发会停止双方负载。
- [ ] 1.4 实现双模块 PID/进程组回收、预检失败阻断、运行失败/超时/偏差超限的双向停止，并以受控 mock 或 Shell 合约测试验证无残留同伴进程。
- [ ] 1.5 生成包含 GPU、CPU/内存和同步校验明细的机器可解析总结果、TXT/XLSX 总报告及子模块证据清单，并以报告生成测试验证 PASS、单模块 FAIL 和同步 FAIL。

## 2. 后端任务模型与生命周期

- [ ] 2.1 将极限模式作为受控的单一 stress 任务输入，扩展参数校验、脚本白名单、命令构建和远端工作目录处理，并以 API/参数测试验证不会创建磁盘子任务。
- [ ] 2.2 为极限任务增加报告摘要解析和最终状态聚合，使仅在双模块及同步校验均 PASS 时返回 PASS，并以任务序列化测试验证失败原因优先级和部分证据保留。
- [ ] 2.3 将取消逻辑扩展为通过编排器进程组同时回收两个模块，并以取消路径测试验证任务最终为 CANCELED 且不遗留远端子进程。
- [ ] 2.4 扩展重试和启动恢复，使极限任务只可整体重试，并在恢复时重新监控远端编排进程而不重复下发负载；以恢复与重试回归测试验证。

## 3. 任务下发与历史展示

- [ ] 3.1 在服务器压测下发页增加“常规压测 / 极限压测”互斥模式，极限模式固定 GPU + CPU/内存并隐藏磁盘与全选入口；以前端单元测试验证切换时清除常规脚本和磁盘选择。
- [ ] 3.2 更新极限模式的执行计划、风险提示、命令预览、按钮文本和创建请求，并以组件测试验证常规全选仍只选择三个既有脚本且保持串行。
- [ ] 3.3 在历史任务列表和详情中识别极限原子任务，展示“极限压测 · GPU + CPU/内存（同步）”、整体状态及 GPU/CPU/内存/同步明细，并以前端展示测试验证 PASS、单模块 FAIL 和同步 FAIL。
- [ ] 3.4 将极限任务的重试入口限制为整体重试，并在详情提供总报告、两个子报告和原始日志入口；以历史交互测试验证不出现模块级重试。

## 4. 文档与集成验证

- [ ] 4.1 更新 `backend/scripts/` 相关文档、`README.md` 或 `docs/architecture.md` 以及 `docs/progress.md`，记录极限模式、依赖准备、15% 内存保留线、同步阈值和常规模式兼容边界；以文档检索验证内容一致。
- [ ] 4.2 运行后端、前端和 Shell 相关定向回归测试，以及 `bash -n`、前端构建和 OpenSpec 严格校验；记录通过项与未覆盖的真实硬件联调项。
- [ ] 4.3 在具备 NVIDIA GPU 的非生产验证服务器上执行一次短时极限压测，核验两负载启动偏差、三类报告、取消回收及历史展示；生产发布、Nginx 重载或服务重启须另行确认。
