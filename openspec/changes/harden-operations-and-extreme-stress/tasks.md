## 1. SSH 主机身份与认证迁移

- [x] 1.1 为服务器记录增加受信 SSH 指纹、算法、确认时间和密钥认证验证状态，并实现兼容现有 SQLite 数据的初始化迁移；验证现有服务器记录可读取且默认为待确认状态。
- [ ] 1.2 抽取共享 SSH host key 采集与校验组件，替换探测、任务执行和公钥部署入口的静默 `AutoAddPolicy` 行为；验证未知、已匹配和不匹配指纹三种连接测试通过。
- [ ] 1.3 增加首次指纹读取/确认及指纹变更确认 API，并写入不含敏感值的审计记录；验证未确认或不匹配时远端变更和任务下发均被拒绝。
- [ ] 1.4 在服务器管理界面展示待确认、已确认和指纹变更状态，提供旧/新指纹对比与确认更新交互；验证首次密码接入不被取消且确认前无危险操作。
- [ ] 1.5 实现密码完成公钥部署后的密钥连接验证、切换认证方式和确认式清除平台留存密码 API/UI；验证清除只影响本地密码字段、不影响远端公钥与服务器其他信息。
- [ ] 1.6 补充服务器认证、指纹迁移、变更阻断和密码清除的后端/前端回归测试；运行相关 pytest 与 frontend 测试通过。

## 2. 极限压测预检与资源互斥

- [ ] 2.1 定义极限压测预检响应和任务证据结构，包含身份、连接、GPU、依赖、内存余量、监控、远端/控制器存储和冲突任务；验证 API 能返回 pass、warning、blocked 的逐项结果。
- [ ] 2.2 实现只读预检 API，并在极限压测创建 API 中执行同等的服务端最终校验；验证浏览器绕过或预检过期时 blocked 条件仍拒绝创建任务。
- [ ] 2.3 实现服务器加资源域的任务冲突判定，覆盖极限、GPU、CPU/内存任务及重试/恢复路径；验证冲突请求不产生远端命令并返回冲突任务和资源类别。
- [ ] 2.4 在任务下发界面展示预检结果、可自动准备依赖、警告和阻断原因，并在冲突时给出可定位的任务链接/标识；验证常规压测流程不受影响。
- [ ] 2.5 将预检快照、自动依赖准备和资源冲突结论写入任务日志/参数/审计证据；验证完成后历史任务可读到完整提交前依据。
- [ ] 2.6 为预检、并发提交互斥、历史兼容和极限压测提交补充单元及 API 回归测试；运行相关 pytest 与 frontend 测试通过。

## 3. 极限压测异常安全收尾

- [ ] 3.1 为极限压测补充可信温度采样、连续超温阈值和连续连接失败阈值的受控配置；验证监控缺失仅产生 warning，不伪造温度保护结论。
- [ ] 3.2 实现超温、取消和连接失联时的原子停止/远端进程确认流程，复用现有组合任务的停止逻辑；验证任一触发都不会单独留下 GPU 或 CPU/内存模块运行。
- [ ] 3.3 定义并展示“已确认停止”“远端状态未确认”“远端清理失败”的任务结果与人工处置建议；验证 SSH 不可达时不会错误报告已清理。
- [ ] 3.4 将最后心跳、PID/结果清单证据和处置尝试保存到任务历史；验证后端重启恢复仍只监控远端任务、不重复下发压力负载。
- [ ] 3.5 补充温度保护、取消、失联和启动恢复的回归测试；在测试机完成一次可控的极限压测取消/恢复演练并记录结论。

## 4. 历史基线与容量可观测性

- [ ] 4.1 提取可比较压测的稳定摘要指标和参数签名，定义同服务器同类别最近 SUCCESS 基线查询；验证不同服务器、不同类别或参数不兼容的任务不会被比较。
- [ ] 4.2 实现可配置的退化阈值和保守比较结果，包含基线任务 ID、比较指标和“首个基线/证据不足”状态；验证数据缺失不生成退化结论。
- [ ] 4.3 在历史任务中展示基线比较、极限压测预检及异常处置证据；验证既有历史任务在缺少新字段时仍可正常渲染。
- [ ] 4.4 实现控制器数据分区余量、产物占用、SQLite 备份占用和自动清理状态的统一统计与两级容量阈值；验证 warning 不影响运行任务，blocking 阈值拒绝新极限压测。
- [ ] 4.5 在仪表盘/设置或任务下发页显示容量告警、占用构成和上次清理结论；验证低空间状态能被清晰识别并提供清理或扩容建议。
- [ ] 4.6 为基线比较、容量阈值、历史兼容和前端展示补充回归测试；运行相关 pytest、frontend 测试与构建通过。

## 5. 前端独立发布与运维文档

- [x] 5.1 设计并实现受控前端独立发布脚本，构建到临时版本目录、校验 Nginx、原子切换静态资源并保留上一版本；验证失败时当前版本不变且脚本返回非零。
- [ ] 5.2 保持完整发布脚本的活动任务保护，并为前端独立发布明确不重启后端、不停止任务的检查；验证有 RUNNING 任务时完整发布仍拒绝、独立发布可成功。
- [ ] 5.3 更新 `deploy/README.md`、项目 `README.md` 或 `docs/architecture.md`、`docs/progress.md`，记录指纹确认、极限压测保护、容量阈值和前端独立发布的行为、边界与回滚方式；验证文档命令和路径与脚本一致。
- [ ] 5.4 在测试环境执行一次前端独立发布与回滚演练，同时确认活动任务状态和后端健康接口保持正常；记录验证结论。

## 6. 集成验证与交付

- [ ] 6.1 运行完整后端测试、前端测试与生产构建，修复本变更引入的失败；验证无新增回归。
- [ ] 6.2 执行 SSH 身份确认、密码转密钥、极限压测预检/冲突/取消、低空间阻断和前端热发布的端到端验收；验证结果与各 delta spec 场景一致。
- [ ] 6.3 运行 `openspec validate --change harden-operations-and-extreme-stress --strict`，并在交付中说明工作区、Git 提交、构建产物和发布分别是否完成。
