# HPCDeploy 项目规则

1. 等待确认时，用户单独回复 `1` 即确认紧邻上一条已说明的操作范围，不自动扩大范围。涉及代码、交互或脚本行为的改动，用户点名的 A 是默认实施范围。先检索同类入口、单次/批次、前后端、脚本和测试，发现共性缺陷或重复逻辑时先报告影响、方案、文件、风险和验证；未经确认不得同步扩改、批量修改或抽取重构。纯文案、格式和显然局部的样式调整不触发完整横向检索。
2. 前端按专业运维控制台交付：功能、交互反馈、文案、按钮语义、信息层级、状态/加载/空态/失败态及跨页面一致性都应直观、舒适、低认知负担。发现体验问题先给方案和影响页面，确认后再改。
3. 更新 `backend/scripts/` 时，同次同步相关文档：记录版本、行为变化和兼容性/边界；涉及当前能力时更新 `README.md` 或 `docs/architecture.md`，并更新 `docs/progress.md`，完成后验证。
4. 改动完成后必须明确报告发布状态。工作区修改、Git 提交、构建产物和 Nginx `/var/www` 线上文件视为不同阶段；未发布时，说明未发布并询问是否按项目部署流程发布。未经用户确认不得发布、重载 Nginx 或重启服务。
