# HPCDeploy MVP 验收清单

> 本文档用于 HPCDeploy MVP 功能验收。核心主链路：服务器管理 → 脚本知识库 → 选择服务器 → 选择脚本 → SSH 上传 → 执行脚本 → 实时日志 → 资源快照 → 任务历史 → 结果文件回收 → 仪表盘总览。

---

## 1. 先决条件

- [ ] 后端可启动（`cd backend && python main.py`）
- [ ] 前端可启动（`cd frontend && npm run dev`）
- [ ] 数据库已初始化（`backend/data/hpc_control_panel.db` 存在）
- [ ] 测试脚本存在（`backend/scripts/test/hello.sh`）
- [ ] 压测脚本存在（`backend/scripts/stress/cpu_mem_stress_report.sh`）
- [ ] 远端服务器已添加本机 SSH 公钥

---

## 2. 服务器管理

- [ ] 新增服务器成功后出现在列表中，状态为 `unknown`
- [ ] SSH 测试成功后状态变为 `online`（失败后变为 `offline`）
- [ ] 支持 `SSH Key / Password` 两种认证方式
- [ ] SSH 私钥从 `backend/keys/` 下拉选择，不手写路径
- [ ] `Password` 登录服务器支持一键部署公钥
- [ ] 部署公钥成功后自动切换为 `SSH Key`
- [ ] 探测信息能显示 OS/CPU/内存/GPU/磁盘摘要
- [ ] 详情弹窗能看到最后探测时间和最后错误
- [ ] 编辑服务器后列表同步更新
- [ ] 删除服务器后列表消失

---

## 3. 脚本知识库

- [ ] 4 个分类页签显示正确：全部 / 编译环境(mpi) / 压测脚本(stress) / 测试脚本(test) / Apptainer 容器(apptainer)
- [ ] 各分类页签能正确过滤文件
- [ ] 上传 `.sh` 文件到指定分类后文件出现在列表中
- [ ] 预览文本文件内容正常
- [ ] 下载脚本文件正常
- [ ] 删除脚本文件后列表更新
- [ ] `.sif` 文件只显示文件信息，不尝试预览内容
- [ ] 上传非 `.sh`/`.py`/`.sif` 文件被拒绝

---

## 4. test/hello.sh 执行

- [ ] 选择服务器后显示服务器名 + 主机名 + 状态标签
- [ ] 选择"测试脚本"类型后文件列表正确过滤
- [ ] 选择 hello.sh 后命令预览显示 `bash ./hello.sh`
- [ ] 校验参数通过后弹出"参数校验通过"
- [ ] 点击"开始执行"后切换到摘要模式右侧日志开始滚动
- [ ] 等待 5-10 秒后状态变为 SUCCESS，退出码为 0
- [ ] 日志中包含 SYSTEM 级别的连接/上传/执行日志和 INFO 级别的 stdout

---

## 5. stress 60 秒执行

- [ ] 点击"新建任务"能清空状态回到 config 模式
- [ ] 选择"压测脚本"类型后文件列表正确过滤，参数区出现"压测时长"
- [ ] 设置 0 小时 / 1 分钟 / 0 秒，总秒数显示为 60
- [ ] 执行后切换到实时面板，日志持续滚动
- [ ] CPU/内存资源快照能刷新显示 top + free + ps 输出
- [ ] 磁盘 IO 资源快照能刷新显示 iostat + df 输出
- [ ] GPU 资源快照能刷新显示 nvidia-smi 输出（或无 GPU 提示）
- [ ] 等待 60 秒后状态变为 SUCCESS，退出码为 0
- [ ] RUNNING 状态下"开始执行"按钮灰色禁用

---

## 6. mpi/apptainer 阶段边界

- [ ] mpi 脚本已开放执行（mpi_env_test.sh / install_oneapi_2022.sh / install_openmpi_4.1.6_aocc_aocl.sh）
- [ ] apptainer 任务选择 `.sif` 文件后上传到 `$HOME/hpcdeploy/apptainer/`
- [ ] apptainer 日志包含分发完成提示
- [ ] apptainer 不做 run/exec，不上传 .sh/.py 文件

---

## 7. 任务取消

- [ ] RUNNING/PENDING 等未完成任务显示"取消任务"按钮
- [ ] 取消弹窗提示清晰，确认后执行
- [ ] 取消后远端进程通过 `.hpcdeploy.pid` 定位 PGID 并终止
- [ ] 先 SIGTERM 等待，超时后 SIGKILL
- [ ] 远端工作目录 `~/hpcdeploy/tasks/{type}/{task_id}/` 被清理
- [ ] 脚本对应的临时下载目录（`/tmp/oneapi_install_2022`、`/tmp/openmpi_aocc_aocl_install`）被清理
- [ ] 任务状态变为 CANCELED，exit_code 为 -15
- [ ] 任务记录和日志不删除
- [ ] 已安装到 `/opt`、`/usr` 的软件不回滚
- [ ] CANCELING 状态下按钮灰色禁用

---

## 8. 任务删除

- [ ] SUCCESS / FAILED / CANCELED 状态任务显示"删除任务"按钮
- [ ] 未完成任务不显示删除按钮（显示取消按钮）
- [ ] 删除确认弹窗采用结构化排版（"将删除" / "不会删除"）
- [ ] 点击确认后任务从历史列表中消失
- [ ] 远端工作目录被清理
- [ ] 本地 artifacts 目录被清理
- [ ] 任务日志（task_logs 表）被删除
- [ ] 服务器配置、脚本知识库文件、已安装到 `/opt`/`/usr` 的软件不受影响

---

## 9. 任务历史筛选/搜索/分页

- [ ] 进入任务历史页，默认显示最近 50 条任务
- [ ] 页面底部显示分页组件，显示总数和当前页码
- [ ] 状态下拉筛选正确
- [ ] 类型下拉筛选正确
- [ ] 关键词搜索能匹配 task_id / 文件路径 / 远端目录 / 错误信息
- [ ] 点击重置后筛选条件清空
- [ ] 分页切换正常
- [ ] 每页条数切换（20/50/100）正常
- [ ] 搜索按钮有筛选条件时变蓝色 primary

---

## 10. 日志下载

- [ ] 任务历史页面每个任务都有"下载日志"按钮
- [ ] 下载文件名格式为 `{task_id}.log`
- [ ] 下载内容包含所有日志（SYSTEM/INFO/ERROR）
- [ ] 下载不依赖任务状态

---

## 11. 结果文件回收下载

- [ ] stress 已完成任务显示"结果文件"按钮
- [ ] 结果文件弹窗显示文件列表 + 本地保存目录 + 下载按钮
- [ ] 下载文件正常
- [ ] 空结果文件时显示提示信息

---

## 12. 仪表盘统计

- [ ] 仪表盘显示服务器总数/在线/离线
- [ ] 仪表盘显示任务运行中/等待中/取消中
- [ ] 仪表盘显示任务成功/失败/已取消
- [ ] 仪表盘显示结果文件归档目录数量和占用空间
- [ ] 最近任务显示业务任务名称：`客户名 · 模块 · 脚本名 · 日期`
- [ ] 点击刷新按钮重新加载数据
- [ ] 加载失败显示错误提示

---

## 13. 仪表盘快捷操作

- [ ] 8 个按钮分组展示（任务/资源两组）
- [ ] 新建任务 → 任务执行页
- [ ] 运行中任务 → 任务历史（筛选运行中）
- [ ] 失败任务 → 任务历史（筛选失败）
- [ ] 已取消任务 → 任务历史（筛选已取消）
- [ ] 最近任务 → 任务历史
- [ ] 服务器管理 → 服务器页
- [ ] 脚本知识库 → 脚本页
- [ ] 结果文件归档 → 打开目录树

---

## 14. 结果文件归档目录查看

- [ ] 点击"查看目录"打开 el-drawer
- [ ] el-tree 展示归档目录结构
- [ ] 显示目录数量、总占用空间、展示深度
- [ ] 目录较多时显示截断提示

---

## 15. 最近任务跳转任务历史

- [ ] 仪表盘最近任务表格整行可点击
- [ ] 点击后跳转到任务历史页并按 task_id 搜索
- [ ] 搜索条件自动填入

---

## 16. 布局对齐检查

- [ ] 左侧导航右边框是一条完整竖线
- [ ] 顶部栏左边界从左侧导航右边界开始
- [ ] 内容区左边界从左侧导航右边界开始
- [ ] 顶部栏下边框横向连续不错位
- [ ] 切换页面后仍然对齐
- [ ] 页面滚动时顶部栏 sticky 可见
- [ ] 页面滚动时左侧栏 fixed 可见
- [ ] 无横向滚动条

---

## 17. 安全边界检查

- [ ] 前端 createTask payload 无 command / remote_path / timeout
- [ ] 前端不传 local_path / PID / kill command
- [ ] 知识库脚本路径无法逃逸
- [ ] artifact 下载防路径逃逸
- [ ] 远端文件名通过 _safe_basename() 校验
- [ ] 回收失败不影响任务状态
- [ ] 只允许回收白名单后缀
- [ ] 删除任务远端路径安全格式校验
- [ ] SSH/安全失败不删数据库记录
- [ ] `/api/ssh-keys` 不返回私钥/公钥内容
- [ ] 部署公钥只写远端 `~/.ssh/authorized_keys`
- [ ] 不覆盖 `authorized_keys`
- [ ] 不修改 `sshd_config`
- [ ] 不重启 `sshd`
- [ ] 服务器探测只执行固定安全命令

---

## 18. 非阻塞问题记录

### 18.1 clipboard.writeText 在非 HTTPS 环境降级
TaskHistory.vue 中 `copyArtifactDir` 使用 `navigator.clipboard.writeText()`，需要安全上下文。开发环境（localhost）没问题，生产需要 HTTPS。

### 18.2 旧 PENDING 任务需要人工清理
同服务器防重复提交把旧 PENDING 也视为未完成任务，可通过 SQLite 手动修正为 FAILED。

### 18.3 回收失败未更新 error_message
artifact_collector.py 回收失败只写 ERROR 日志，没有更新 task.error_message。

---

## 19. MVP 验收通过标准

- [ ] test/hello.sh 能执行成功
- [ ] stress 60 秒能执行成功
- [ ] mpi_env_test.sh 能执行成功
- [ ] 实时日志正常
- [ ] stress 结果文件能回收并下载
- [ ] RUNNING 任务能取消
- [ ] 终态任务能删除
- [ ] 筛选/搜索/分页正常
- [ ] 仪表盘数据正常
- [ ] 知识库路径和 artifact 下载路径不能逃逸
- [ ] 日志下载功能正常
- [ ] 布局对齐无缝隙
